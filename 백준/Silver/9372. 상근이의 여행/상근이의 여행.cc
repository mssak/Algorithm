#include<bits/stdc++.h>
#define MAX 1'001
using namespace std;

int main()
{
    cin.tie(NULL);
    ios_base::sync_with_stdio(false);

    int T;
    cin>>T;

    while(T--){
        int n,m;
        cin>>n>>m;
        for(int i=0;i<m;i++){
            int a,b;
            cin>>a>>b;
        }
        cout<<n-1<<"\n";
    }
}
